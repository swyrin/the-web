name: arknights-vns
services:
  dreamchasers:
    build: .
    container_name: dreamchasers
    hostname: dreamchasers
    ports:
      - "3000:3000"
    restart: unless-stopped
    env_file:
      - .env
